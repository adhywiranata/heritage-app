import React from 'react';
import {
  TouchableOpacity,
  Text,
} from 'react-native';

const ButtonUI = () => (
  <TouchableOpacity>
    <Text>Dis is a Button</Text>
  </TouchableOpacity>
);

export default ButtonUI;
